---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: deployments.deployments.kuberik.com
spec:
  group: deployments.kuberik.com
  names:
    kind: Deployment
    listKind: DeploymentList
    plural: deployments
    singular: deployment
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Deployment is the Schema for the deployments API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of Deployment
            properties:
              backendConfig:
                description: BackendConfig contains backend-specific configuration
                properties:
                  backend:
                    description: Backend specifies the deployment backend to use (e.g.,
                      "github")
                    enum:
                    - github
                    type: string
                  backendSecret:
                    description: BackendSecret is the name of the Kubernetes Secret
                      containing the backend authentication token
                    type: string
                  project:
                    description: Project is the project identifier (backend-specific
                      format, e.g., "owner/repo" for GitHub)
                    type: string
                required:
                - backend
                - project
                type: object
              deploymentName:
                description: DeploymentName is the name of the deployment
                type: string
              environment:
                description: Environment is the deployment environment (e.g., "production",
                  "staging")
                type: string
              relationship:
                description: Relationship defines how this deployment relates to another
                  environment
                properties:
                  environment:
                    description: Environment is the environment name this deployment
                      relates to
                    type: string
                  type:
                    description: 'Type is the type of relationship: "after" or "togetherWith"'
                    enum:
                    - after
                    - togetherWith
                    type: string
                required:
                - environment
                - type
                type: object
              requeueInterval:
                description: |-
                  RequeueInterval specifies how often the controller should reconcile this Deployment
                  If not specified, defaults to 1 minute. Must be a valid duration string (e.g., "1m", "30s", "5m").
                pattern: ^([0-9]+(\.[0-9]+)?(ms|s|m|h))+$
                type: string
              rolloutRef:
                description: RolloutRef is a reference to the Rollout that this Deployment
                  manages
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
            required:
            - backendConfig
            - deploymentName
            - rolloutRef
            type: object
          status:
            description: status defines the observed state of Deployment
            properties:
              conditions:
                description: |-
                  conditions represent the current state of the Deployment resource.
                  Each condition has a unique type and reflects the status of a specific aspect of the resource.

                  Standard condition types include:
                  - "Available": the resource is fully functional
                  - "Progressing": the resource is being created or updated
                  - "Degraded": the resource failed to reach or maintain its desired state

                  The status of each condition is one of True, False, or Unknown.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              currentVersion:
                description: CurrentVersion is the current version being deployed
                type: string
              deploymentId:
                description: DeploymentID is the ID of the deployment (backend-specific)
                format: int64
                type: integer
              deploymentStatuses:
                description: |-
                  DeploymentStatuses tracks deployment statuses per version and environment.
                  Only versions that are relevant based on environment relationships are tracked.
                items:
                  description: DeploymentStatusEntry represents a deployment status
                    for a specific version in an environment.
                  properties:
                    deploymentId:
                      description: DeploymentID is the deployment ID (backend-specific)
                      format: int64
                      type: integer
                    deploymentUrl:
                      description: DeploymentURL is the URL of the deployment (backend-specific)
                      type: string
                    environment:
                      description: Environment is the deployment environment (e.g.,
                        "production", "staging")
                      type: string
                    status:
                      description: Status is the deployment status state (e.g., "success",
                        "failure", "in_progress", "pending", "inactive")
                      type: string
                    version:
                      description: Version is the version/revision being tracked
                      type: string
                  required:
                  - environment
                  - status
                  - version
                  type: object
                type: array
                x-kubernetes-list-type: atomic
              deploymentUrl:
                description: DeploymentURL is the URL of the deployment (backend-specific)
                type: string
              environmentInfos:
                description: |-
                  EnvironmentInfos tracks deployment information for each environment.
                  Each environment has environment URL and relationships (not per version).
                items:
                  description: EnvironmentInfo represents information about an environment's
                    deployment.
                  properties:
                    environment:
                      description: Environment is the environment name
                      type: string
                    environmentUrl:
                      description: EnvironmentURL is the URL of the actual deployed
                        environment (e.g., dashboard URL)
                      type: string
                    relationship:
                      description: Relationship defines how this environment relates
                        to another environment
                      properties:
                        environment:
                          description: Environment is the environment name this deployment
                            relates to
                          type: string
                        type:
                          description: 'Type is the type of relationship: "after"
                            or "togetherWith"'
                          enum:
                          - after
                          - togetherWith
                          type: string
                      required:
                      - environment
                      - type
                      type: object
                  required:
                  - environment
                  type: object
                type: array
                x-kubernetes-list-type: atomic
              lastSyncTime:
                description: LastSyncTime is the last time the deployment was synchronized
                format: date-time
                type: string
              rolloutGateRef:
                description: RolloutGateRef is a reference to the RolloutGate that
                  was created/updated
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
